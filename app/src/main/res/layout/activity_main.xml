<!-- res/layout/activity_main.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/editTextTask"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter your task"/>

    <Button
        android:id="@+id/btnAddTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextTask"
        android:layout_marginTop="8dp"
        android:text="Add Task"/>

    <Button
        android:id="@+id/btnClearTasks"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/listViewDoneTasks"
        android:layout_marginTop="16dp"
        android:text="Clear Tasks"/>

    <TextView
        android:id="@+id/textViewDueTasksCaption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Due Tasks"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_below="@id/btnAddTask"
        android:layout_marginTop="16dp"/>

    <ListView
        android:id="@+id/listViewDueTasks"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/textViewDueTasksCaption"
        android:layout_marginTop="8dp"/>

    <TextView
        android:id="@+id/textViewDoneTasksCaption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Done Tasks"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_below="@id/listViewDueTasks"
        android:layout_marginTop="16dp"/>

    <ListView
        android:id="@+id/listViewDoneTasks"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/textViewDoneTasksCaption"
        android:layout_marginTop="8dp"/>

</RelativeLayout>
